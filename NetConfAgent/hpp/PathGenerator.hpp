#pragma once
#include<string>

namespace PathGenerator
{
    const std::string _MODEL_NAME("comьutator");
    std::string generatePathNumber(const std::string & number);
    std::string generatePathName(const std::string & number);
}